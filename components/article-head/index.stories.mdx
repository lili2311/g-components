import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { DEFAULTS } from '../layout/index.stories.mdx';
import '../../shared/critical-path.scss';
import ArticleHead from './';

<Meta title="Core|Article Head" component={ArticleHead} />

# ArticleHead component

tktk

<Preview>
  <Story name="Basic">
    <ArticleHead
      flags={DEFAULTS.flags}
      url={DEFAULTS.url}
      headline="Testing <ArticleHead> component"
      summary={DEFAULTS.summary}
      mainImage={DEFAULTS.image}
      topic={DEFAULTS.topic}
      bylines={DEFAULTS.bylines}
      publishedDate={DEFAULTS.pubdate}
      buildTime={DEFAULTS.buildTime}
    />
  </Story>
</Preview>
